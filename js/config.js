// ====================================================================
// TEAM SETUP — paste your Supabase project details between the quotes.
//
// 1. Go to https://supabase.com → your project → Settings → API
// 2. Copy "Project URL"        → SUPABASE_URL
// 3. Copy "anon public" key    → SUPABASE_ANON_KEY
//
// The anon key is designed to be public (it ships to every visitor's
// browser). Access is controlled by the database policies in
// supabase/schema.sql. Never paste the "service_role" key here.
//
// Until both values are filled in, the app runs in LOCAL MODE:
// everything works and survives refresh, but only on this computer —
// it is not shared with the team.
// ====================================================================

export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
