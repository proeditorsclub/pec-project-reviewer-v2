// ====================================================================
// The app runs in LOCAL MODE by default: all review marks are saved in
// this browser and survive refresh. That's the intended setup for a
// single reviewer.
//
// OPTIONAL — shared team sync (only if several people must see the
// same marks): create a free Supabase project, run supabase/schema.sql
// in its SQL editor, then paste its Project URL and "anon public" key
// below (Settings → API). Never paste the "service_role" key here.
// ====================================================================

export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
